<% @users.each do |user| %>
 <ul>
    <li><%= user.username %></li>
    <li><%= user.email %></li>
    <li><%= user.admin %></li>
    <%= button_to "delete", {:controller => :admins,
      :action => 'destroy', :id => user.id }, :method => :delete %>
  </ul>
<% end %>

